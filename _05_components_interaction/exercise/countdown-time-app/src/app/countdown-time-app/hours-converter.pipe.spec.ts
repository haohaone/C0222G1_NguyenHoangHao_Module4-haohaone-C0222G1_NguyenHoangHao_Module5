import { HoursConverterPipe } from './hours-converter.pipe';

describe('HoursConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new HoursConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
